#!/usr/bin/env perl

use v5.20.0;
use strict;
use warnings;
use Liguros::CPUinfo;

my $cpu = Liguros::CPUinfo->new();
say "hello world";
say $cpu->cpu_file;
say @{ $cpu->errors };
say $cpu->timer;
say $cpu->model;
say $cpu->MHz;
say $cpu->processors;
say $cpu->get_json;

